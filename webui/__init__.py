"""TradingAgents Web Console — a localhost dashboard over the library.

Complements the interactive CLI: browse persisted analyses, launch new
runs in isolated subprocesses, inspect the decision log and effective
configuration. Stdlib-only by design (no web framework, no build step).
"""
